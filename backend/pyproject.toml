[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
    ".venv",
    "*/migrations/*",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "DJ"]

[tool.ruff.format]
quote-style = "double"
