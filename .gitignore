# Python
__pycache__/
*.py[cod]
*.sqlite3
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Django
staticfiles/
media/
backend/media/

# Node
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# IDE
.vscode/
.idea/

# Claude Code / agent config (personal, machine-specific; not shared)
.claude/
.claude/settings.local.json

# AI-workflow working artifacts — kept on disk, never committed (senior-review
# reports, PR handoff docs, etc.). Product documentation lives in docs/.
review/

# Tooling caches
.ruff_cache/

# OS
.DS_Store
Thumbs.db
# Windows "mark of the web" alternate-data-stream files (created on WSL/NTFS)
*:Zone.Identifier

# Isometric asset SOURCES (PR 080): multi-MB base64-PNG-in-SVG originals. The pipeline
# emits optimized WebP into frontend/src/assets/iso/ (that output IS committed). Owner keeps
# his own backup of the sources.
assets-src/
